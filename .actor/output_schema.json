{
    "actorOutputSchemaVersion": 1,
    "title": "theHarvester results",
    "description": "Every finding is one row with a recordType: host (subdomain), email, ip, url, asn, shodan, person, and one summary row at the end with totals and input notes.",
    "type": "object",
    "properties": {
        "results": {
            "type": "string",
            "title": "All results (results)",
            "description": "Every row the run found. Use the Subdomains, Emails and Run summary views, or filter by recordType.",
            "template": "{{links.apiDefaultDatasetUrl}}/items"
        },
        "screenshots": {
            "type": "string",
            "title": "Page screenshots (screenshots)",
            "description": "Screenshots of each subdomain's home page, saved when Page screenshots is on. Empty otherwise.",
            "template": "{{links.apiDefaultKeyValueStoreUrl}}/keys"
        }
    }
}
